### 模块中存储函数

- 模块的定义

  模块是包含 Python 代码的文件，其中仅包含函数定义，不包含函数调用；函数调用由导入该模块的程序完成。

- 模块命名规则

  - 文件名需以`.py`结尾。
  - 不能与 Python 关键字同名。

- 模块的导入

  使用`import`语句导入模块。
  
- 模块的双重用途
  可以创建既能作为独立程序运行，又能被导入到其他程序中的模块。
- 场景示例
  假设程序 A 定义了若干函数，需在程序 B 中使用，此时可将程序 A 导入到程序 B 中。
